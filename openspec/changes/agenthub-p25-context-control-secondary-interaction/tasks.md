## 1. 上下文项模型与标准化

- [x] 1.1 添加后端上下文条目辅助函数，将遗留的选定制品/引用消息字段标准化为 `contextItems`。
- [x] 1.2 添加上下文条目标题、摘要、选定文本、元数据和笔记内容的脱敏与限制。
- [x] 1.3 添加后端测试，涵盖标准化兼容性、脱敏、截断及不支持的上下文条目处理。

## 2. 规划器上下文交接

- [x] 2.1 将会话上下文包、规范上下文以及提供者可见的规划器上下文中的标准化上下文项纳入其中。
- [x] 2.2 在 planner/mission 证据中适当记录上下文项数量、种类、制品 ID、版本 ID、部署 ID 以及编辑状态。
- [x] 2.3 添加后端测试，证明上下文感知的后续操作能够到达规划器可见的上下文，而不会绕过 PlanValidator 或泄露受保护数据。

## 3. 上下文托盘界面

- [x] 3.1 将单个上下文芯片替换为紧凑型上下文托盘，可容纳多条消息、制品、部署、选中文本和笔记项。
- [x] 3.2 添加移除、重新排序、清除和发送载荷行为，同时保留现有的引用和制品上下文操作。
- [x] 3.3 添加前端测试，覆盖添加多个上下文项、清除、移除、重新排序以及载荷形状。

## 4. 次要交互操作

- [x] 4.1 在现有 message/artifact/deployment 卡片暴露上下文操作的位置，添加针对“询问此内容”、“据此修订”和“发送给代理”的聚焦 UI 操作。
- [x] 4.2 确保带上下文的纯聊天后续不会创建 TaskRun，而编码后续仍通过对话路由器/规划器进行路由。
- [x] 4.3 为次要交互操作标签、编辑器暂存和路由负载添加测试。

## 5. P25 演练与冻结评审

- [x] 5.1 使用部署卡、差异制品、制品版本和引用消息作为组合上下文，运行一次有边界的预演。
- [x] 5.2 验证 P6-P24 基线路径仍然通过。
- [x] 5.3 记录限制、验证和冻结状态。
