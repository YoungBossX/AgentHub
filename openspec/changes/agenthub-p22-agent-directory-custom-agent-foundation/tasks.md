## 1. Agent 目录后端

- [x] 1.1 添加派生自 Agent 目录条目的模型和服务辅助函数，用于内置配置文件、提供商配置、运行时选择和草稿元数据。
- [x] 1.2 添加工作区范围的 Agent 目录 API 响应，不暴露机密信息。
- [x] 1.3 添加后端测试，覆盖内置条目、运行时选择的角色、提供商可用性以及无机密元数据。

## 2. 兼容性策略

- [x] 2.1 为角色、提供者、适配器、目标、模式、能力及安全标志添加显式兼容性检查。
- [x] 2.2 添加一个 API 或响应元数据路径，用于展示兼容性原因和警告。
- [x] 2.3 为兼容的 frontend/backend/review/planner 选择和不兼容的 role/adapter/target/capability 情况添加后端测试。

## 3. 安全草稿代理管理

- [x] 3.1 扩展草稿代理 API，以支持仅元数据状态的目录安全 create/list 行为。
- [x] 3.2 确保草稿拒绝 shell 命令、不安全的工具权限、不受限制的文件系统访问以及未经验证的写入执行。
- [x] 3.3 添加后端测试，用于安全草稿创建、适用情况下的 cancellation/no-op 行为以及不安全草稿的拒绝。

## 4. 运行时配置集成

- [x] 4.1 将运行时设置验证与 Agent Directory 兼容性关联，适用于 Planner、Frontend、Backend 和 Review 角色。
- [x] 4.2 确保不兼容或不可用的选择会诚实失败，不会静默兜底。
- [x] 4.3 添加后端测试，验证运行时配置接受兼容的目录条目并拒绝不兼容的条目。

## 5. Agent 目录 UI

- [x] 5.1 添加专用的智能体目录设置页面或标签页，包含联系人风格卡片、提供商徽章、能力、目标、模式、安全标志和可用性。
- [x] 5.2 添加 role/provider/capability/target/status 过滤器，不更改运行时配置。
- [x] 5.3 为智能体卡片、过滤器、不可用状态以及聊天工作区简洁性添加前端测试。

## 6. 草稿 Agent 界面

- [x] 6.1 添加带有保存和取消控件的安全草稿表单。
- [x] 6.2 在验证通过前，将草稿代理显示为禁用或仅审查状态。
- [x] 6.3 为保存、取消、验证错误以及无任意 shell 命令控件添加前端测试。

## 7. P22 演练与冻结评审

- [x] 7.1 运行一次有边界的演练，展示内置条目、兼容的运行时选择、不兼容的拒绝、安全的草稿创建以及 UI 过滤。
- [x] 7.2 验证 P6-P21 基线路径仍然通过。
- [x] 7.3 记录 P22 的限制和冻结状态。
