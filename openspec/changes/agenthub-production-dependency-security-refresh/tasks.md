## 1. Refresh audited production dependencies

- [x] 1.1 Upgrade the vulnerable Next.js and Monaco dependency families,
  enforce the patched DOMPurify floor, regenerate the pnpm lockfile, prove a
  zero-advisory production audit, run Web build and full repository gates,
  document the exact result, and clean reproducible temporary artifacts.
