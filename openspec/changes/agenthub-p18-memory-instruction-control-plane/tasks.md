## 1. P18-1 托管 AGENTS.md / CLAUDE.md 桥接

- [x] 1.1 为 `AGENTS.md` 定义确定性的托管块标记，并保留用户自定义块。
- [x] 1.2 根据 AgentHub 规范记忆、目标摘要、运行时边界和护栏引用，编译 `AGENTS.md` 托管块。
- [x] 1.3 生成一个简短的 `CLAUDE.md` 桥接，在可行时引用或导入 `AGENTS.md`。
- [x] 1.4 确保 `CLAUDE.md` 不重复大型项目规则。
- [x] 1.5 添加针对确定性导出、自定义块保留和幂等重新导出的测试。

## 2. P18-2 内存快照模型

- [x] 2.1 使用 `memorySnapshotId` 添加或规划持久化内存快照模型。
- [x] 2.2 默认将会话与内存快照关联。
- [x] 2.3 在规划器证据、TaskRun 元数据、任务追踪或制品元数据中，酌情记录快照 ID 和相关的 hashes/versions。
- [x] 2.4 确保规划器、Claude Code、Codex 和审查代理在同一个 session/task 链中使用相同的快照。
- [x] 2.5 防止正在运行的 TaskRun 静默切换快照。
- [x] 2.6 添加针对快照一致性和显式会话刷新行为的测试。

## 3. P18-3 规范内存模型

- [x] 3.1 添加记忆项模式，包含作用域、类型、来源、状态、信任级别、目标ID、代理角色、最后使用时间、被取代项和时间戳。
- [x] 3.2 支持状态：活跃、待审核、预热、已归档、已拒绝、已删除。
- [x] 3.3 定义项目规则、用户偏好、决策、模式、反馈、会话摘要和外部建议的记忆类型。
- [x] 3.4 为项目记忆和用户偏好记忆添加 version/hash 元数据。
- [x] 3.5 添加模式验证、状态转换、取代和范围过滤的测试。

## 4. P18-4 内存写入策略与提示注入防护

- [x] 4.1 检测显式的用户记忆写入意图，例如“记住这个”、“以后都这样”和“写入项目规则”。
- [x] 4.2 仅在策略允许且用户确认写入后，创建主动记忆。
- [x] 4.3 为系统发现（如重复构建失败、审查发现、部署失败和重复修复）创建 `pending_review` 候选。
- [x] 4.4 确保普通聊天不会自动成为长期记忆。
- [x] 4.5 阻止文件内容、工具输出、提供者输出、检索内容以及 Claude/Codex 建议在未经审查的情况下成为主动记忆。
- [x] 4.6 添加提示注入防护测试和用户确认测试。

## 5. P18-5 记忆检索 v1

- [x] 5.1 实现或规划 SQLite FTS5 / BM25 风格的关键词检索。
- [x] 5.2 为作用域、目标、角色、状态和时间窗口添加元数据过滤器。
- [x] 5.3 应用 importance/trust 评分、时间衰减、陈旧惩罚、冲突惩罚和令牌成本惩罚。
- [x] 5.4 为规划器和后续任务上下文检索相关记忆。
- [x] 5.5 将嵌入、RRF 和图检索排除在强制性的 P18 范围之外。
- [x] 5.6 为记忆精度@5、状态过滤、目标过滤、角色过滤和陈旧记忆排除添加测试。

## 6. P18-6 外部 Agent 内存扫描

- [x] 6.1 将仓库 `AGENTS.md` 和 `CLAUDE.md` 扫描为外部记忆建议。
- [x] 6.2 规划针对 Claude Code 本地自动记忆和 Codex global/repo 指令的安全扫描。
- [x] 6.3 检测外部建议与 AgentHub 规范记忆之间的冲突。
- [x] 6.4 确保外部建议永远不会自动导入为活跃记忆。
- [x] 6.5 添加针对建议创建、冲突检测和无自动覆盖的测试。

## 7. P18-7 内存管理界面

- [x] 7.1 添加 `/settings/memory` 或等效的设置界面。
- [x] 7.2 显示活跃、待处理、预热、已归档、已拒绝和已删除的记忆。
- [x] 7.3 在策略允许的情况下，允许确认、拒绝、归档、删除和取代操作。
- [x] 7.4 显示来源、范围、目标、代理角色、信任级别、状态以及编译后的输出状态。
- [x] 7.5 在有用的情况下，显示会话当前的 `memorySnapshotId`。
- [x] 7.6 为审查操作、过滤器和快照显示添加 UI 测试。

## 8. P18-8 内存评估与冻结审查

- [x] 8.1 为偏好召回率添加定向评估。
- [x] 8.2 为跨智能体一致性率添加定向评估。
- [x] 8.3 为记忆精度@5添加定向评估。
- [x] 8.4 为过期记忆注入次数添加定向评估。
- [x] 8.5 为提示注入写入阻断率添加定向评估。
- [x] 8.6 为快照一致性率添加定向评估。
- [x] 8.7 通过同一快照演练已保存的偏好或项目规则流向规划器和编码智能体。
- [x] 8.8 运行冻结验证，并在 `docs/project-state.md`、`docs/change-log.md` 以及 P18 冻结审查文档中如实记录限制。

## 9. 明确非目标确认

- [x] 9.1 确认 P18 未实现强制向量数据库、完整知识图谱、RRF 融合、未经审核的自动长期学习、多用户共享记忆、提供商市场、云密钥管理器、生产部署或护栏替换。
- [x] 9.2 确认 Claude Code / Codex 私有记忆不能覆盖 AgentHub 规范记忆。
