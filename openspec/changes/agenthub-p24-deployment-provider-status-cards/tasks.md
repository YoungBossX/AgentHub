## 1. 部署提供者注册表

- [x] 1.1 为本地预发布、手动外部交接、Vercel、Netlify 和自定义静态主机添加部署提供者元数据。
- [x] 1.2 添加 API helpers/routes，用于在不暴露密钥的情况下公开提供者可用性、auth/config 状态、支持的制品类型、支持的目标以及审批要求。
- [x] 1.3 为提供者注册表默认值、不可用的外部提供者、无密钥暴露以及本地预发布兼容性添加后端测试。

## 2. 部署请求与状态证据

- [x] 2.1 添加部署 request/status 辅助函数，用于创建诚实标记为已阻塞、失败、移交或就绪的部署证据。
- [x] 2.2 保留现有本地预览部署行为，同时附加提供者元数据和状态历史。
- [x] 2.3 为缺失提供者配置、需要审批的提供者、源预览关联、logs/status 历史以及禁止虚假成功添加测试。

## 3. 部署状态卡片 UI

- [x] 3.1 升级部署卡片，展示提供商徽章、提供商类型、环境、源制品链接、配置状态、日志和状态历史。
- [x] 3.2 添加本地暂存部署、手动外部交接、copy/open URL 以及将部署作为上下文引用的 UI 操作。
- [x] 3.3 为就绪、阻塞、失败、交接和不可用提供商状态添加前端测试。

## 4. 部署上下文与后续工作

- [x] 4.1 确保部署卡片可作为制品上下文被选中，包含提供者 ID、状态、URL、源 ID、日志摘要和安全元数据。
- [x] 4.2 确保携带部署上下文的后续消息通过现有消息上下文路径到达对话路由器/规划器。
- [x] 4.3 为部署上下文编辑和后续负载添加 backend/frontend 测试。

## 5. P24 预演与冻结评审

- [x] 5.1 运行一次有界预演，创建本地暂存部署卡片和阻塞的外部提供商卡片。
- [x] 5.2 验证 P6-P23 基线路径仍然通过。
- [x] 5.3 记录限制和冻结状态。
