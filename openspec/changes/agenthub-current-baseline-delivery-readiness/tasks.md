## 1. Prove current baseline delivery readiness

- [x] 1.1 Correct and regress the Windows `codex.exe` command-entrypoint guard
  and `pnpm.cmd` Preview launcher resolution;
  execute one fresh bounded real-Codex external-target TaskRun through
  the public workflow; persist exact memory, instruction, provider, scope,
  Diff, Review, Preview, and changed-file evidence; run all available project,
  OpenSpec, documentation, encoding, and whitespace gates; audit the exact
  intended commit set; and clean reproducible temporary artifacts.
