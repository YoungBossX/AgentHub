## 1. 真实预发布部署提供者

- [x] 1.1 P11-1 部署提供者抽象：定义部署提供者接口及结果契约，包含 providerId、providerType、targetId、buildCommand、deployCommand、outputUrl、status 和 logs；适配现有模拟部署路径，使其通过同一部署表面保持可用；为提供者选择、未知提供者拒绝、失败提供者结果及模拟部署兼容性添加测试。
- [x] 1.2 P11-2 目标感知部署配置：扩展目标注册表，使内置演示前端和已注册的外部前端目标能够暴露暂存部署元数据，包括构建命令、输出目录、preview/staging命令（如适用）及允许的提供者 ID；确保无部署配置的目标诚实失败；添加测试，验证前端部署配置从目标元数据读取而非分散的常量。
- [x] 1.3 P11-3 本地暂存部署提供者：实现一个本地暂存提供者，运行目标构建命令，在本地提供构建输出目录，记录可访问的暂存 URL，并报告失败的 build/output/server 启动状态而不伪造成功；为内置演示前端或外部 Vite 示例目标添加后端测试及一个受限的 manual/API 冒烟测试。
- [x] 1.4 P11-4 部署日志与状态制品：持久化部署状态转换（排队、构建中、部署中、就绪、失败、已取消）；在部署制品中记录日志、暂存 URL、目标元数据、源 diff/review/preview 引用及提供者详情；通过现有 artifact/deploy UI 暴露此证据，不删除模拟部署卡片；添加 log/status 可见性及制品关联性测试。
- [x] 1.5 P11-5 部署门禁：当最新审查失败、预览失败、存在目标策略违规或请求生产部署时，阻止暂存部署；保留清晰的仅限暂存标签；为每个被阻止状态及成功的门控暂存部署路径添加测试。
- [x] 1.6 P11-6 P11 端到端演练与冻结审查：为内置演示前端或外部 Vite 示例目标演练真实本地暂存部署，验证暂存 URL 可访问，验证日志和部署制品证据已记录，验证模拟部署在需要时仍可用，验证 P6/P7/P8/P9/P10 基线保持不变，更新项目文档和变更日志，仅在验证通过后标记 P11 准备冻结。

## 2. 明确非目标

- [x] 2.1 确认 P11 未实现生产部署、云提供商令牌管理、Vercel/Netlify/Render 完整集成、Docker/Kubernetes、域名管理、数据库托管、自动回滚、多用户部署审批、提供商市场、PR 创建或不受限制的仓库编辑。

## 3. 验证

- [x] 3.1 运行 `pnpm check`。
- [x] 3.2 运行 `pnpm test`。
- [x] 3.3 运行实现期间添加的任何特定于目标的暂存部署测试。
- [x] 3.4 运行 `git diff --check`。
- [x] 3.5 运行 `openspec validate agenthub-p11-real-staging-deploy-provider --strict`。
