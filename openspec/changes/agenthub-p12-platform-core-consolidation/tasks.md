## 1. Platform Core Consolidation

- [x] 1.1 P12-1 Planner / Orchestrator Boundary: introduce planner service, task graph builder, plan validator, and demo planner boundaries; define PlanDraft with plan ID/version, task graph, dependency edges, agent role, target ID, planned files, and rationale; preserve demo fallback, no-mention orchestrator routing, direct assignment shortcuts, mini CRM/full-stack paths, and unsupported-request honesty; add regression tests for existing P6-P11 planning paths.
- [x] 1.2 P12-2 Canonical Shared Context: formalize CanonicalSharedContext with session, user goal, current task, task graph, target context, safe paths, recent messages, relevant artifacts, latest diff/review/preview/deployment, handoff notes, and guardrails; attach source, visibility, created_at, and trust_level metadata to fields; filter protected host paths/secrets before provider-visible rendering; persist context snapshots to TaskRun metadata or artifact metadata; add tests for context inclusion, filtering, and snapshot persistence.
- [x] 1.3 P12-3 Provider Instruction Adapter: refactor instruction generation into instruction_adapters/base.py, instruction_adapters/codex.py, instruction_adapters/claude_code.py, instruction_adapters/scripted_mock.py, and instruction_adapters/shared_sections.py; render provider-specific instructions from canonical context; preserve CodexAdapter, ClaudeCodeAdapter, ScriptedMockAdapter, target-aware instructions, backend/demo-api behavior, external target behavior, and deterministic fallback behavior; add adapter instruction regression tests.
- [x] 1.4 P12-4 Handoff Artifact: add first-class handoff artifact support with from_task_id, from_task_run_id, from_agent_role, to_task_id, to_agent_role, summary, changed_files, artifact_refs, open_questions, verification_status, risk_notes, and created_at; generate dependency handoff artifacts where useful; include upstream handoff in downstream canonical context; add persistence/API tests.
- [x] 1.5 P12-5 Artifact Reference / Follow-up Context: add ArtifactReference model or normalized concept for diff, review, preview, and deployment artifacts; preserve selected artifact context from message to task to canonical context to provider instruction; support safe follow-up references without document/PPT paragraph references; add tests for selected diff/review/preview/deploy references and unsupported reference rejection.
- [x] 1.6 P12-6 Mission Trace Foundation: define SessionMissionTraceResponse with current_goal, tasks, task_runs, events, artifacts, blockers, and next_actions; expose blocked, waiting_dependency, and waiting_target_lock reasons with explicit navigation references; keep mission trace as a read model, not a scheduler replacement; add API and scheduler-state tests.
- [x] 1.7 P12-7 Web Component Decomposition and Message Actions: split workspace-shell.tsx and task-card-list.tsx into session-sidebar, agent-contact-list, chat-thread, message-composer, mission-panel, artifact-panel, task-card, run-history, run-controls, and artifact-chips components; preserve existing behavior and visual layout; add first-stage message actions for copy, quote as context, use artifact as context, retry failed run, and open artifact when backed by existing APIs; update frontend tests.
- [x] 1.8 P12-8 Artifact Version History Skeleton: add or standardize ArtifactVersion metadata with artifact_id, version, source_task_run_id, parent_artifact_id, git_base_ref, git_head_ref, changed_files, and summary; support v1/v2 follow-up chains for diff/review/preview/deployment evidence; do not implement online editing or git revert UI; add tests for version creation and readback.
- [ ] 1.9 P12-9 Agent Profile Minimal Foundation: stabilize AgentProfile schema with id, display_name, avatar_initials, role, adapter_type, provider_id, capability_tags, supported_targets, supported_modes, safe_for_write, safe_for_review, and description; map existing built-in agents into profiles; do not add user-created custom agents UI; update agent contact/profile tests.
- [ ] 1.10 P12-10 E2E Rehearsal and Freeze Review: run a complete rehearsal covering new session, @orchestrator login-page request, plan/task graph, frontend agent run, diff, handoff, QA review, preview, local staging deploy, follow-up modification, artifact version v2, and updated preview/deploy evidence; verify P6-P11 baselines remain intact; update docs/project-state.md, docs/change-log.md, docs/p12-freeze-review.md, and mark P12 ready to freeze only after validation passes.

## 2. Explicit Non-goals

- [ ] 2.1 Confirm P12 does not implement provider marketplace, full user-created custom agents UI, full artifact editor, document/PPT rendering, multi-user mission control, desktop/IDE/CLI clients, cloud deploy provider, LLM dynamic planner, or scheduler replacement with LangGraph/CrewAI.

## 3. Validation

- [ ] 3.1 Run `pnpm check`.
- [ ] 3.2 Run `pnpm test`.
- [ ] 3.3 Run targeted backend/frontend tests added for each P12 task.
- [ ] 3.4 Run `git diff --check`.
- [ ] 3.5 Run `openspec validate agenthub-p12-platform-core-consolidation --strict`.
