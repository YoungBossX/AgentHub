## 1. P18b-1 场景模型与测试夹具

- [x] 1.1 添加一个记忆排练场景模型，包含角色、请求、预期记忆 ID、排除记忆 ID、角色、目标和评分期望。
- [x] 1.2 添加确定性场景固定数据，用于中文偏好回忆、项目规则合规、陈旧记忆排除和提示注入拦截。
- [x] 1.3 添加测试，证明场景稳定、无私有数据且可解析。

## 2. P18b-2 控制/实验评估引擎

- [x] 2.1 添加一个评估器，在记忆最小化的控制上下文和启用记忆的实验上下文中运行每个场景。
- [x] 2.2 复用 P18 的记忆检索、快照、规划器输入和编码上下文辅助函数，而不是创建并行的记忆栈。
- [x] 2.3 计算 control/treatment 指标，并在没有可比较的任务证据时将任务成功增量标记为未知。
- [x] 2.4 添加针对偏好回忆改进、无虚假任务成功增量、过期排除、提示注入拦截率和快照一致性的测试。

## 3. P18b-3 排练报告与 API 接口

- [x] 3.1 添加结构化演练报告模型，包含场景结果、聚合指标、证据来源标签、提供者可用性及限制。
- [x] 3.2 添加本地 API 或服务入口点，无需实时提供者凭据即可生成报告。
- [x] 3.3 确保 deterministic/fake-client 证据与真实提供者证据分开标注。
- [x] 3.4 添加针对报告序列化、提供者不可用证据及指标限制原因的测试。

## 4. P18b-4 真实工作流冒烟测试

- [x] 4.1 使用保存的用户偏好和项目规则记忆，运行一次有边界的真实演练。
- [x] 4.2 验证普通聊天保持非执行状态，且编码请求在执行时仍能通过 PlanValidator / 调度器边界。
- [x] 4.3 如果真实的 Planner 或编码代理提供商可用，则记录实时证据；否则记录确切的阻塞原因，且不声称实时成功。
- [x] 4.4 记录每个场景中记忆是改善、退化还是无定论。

## 5. P18b-5 冻结评审

- [x] 5.1 使用场景证据、control/treatment 指标、提供商可用性、验证和延迟风险更新 `docs/p18b-freeze-review.md`。
- [x] 5.2 使用 P18b 状态更新 `docs/project-state.md` 和 `docs/change-log.md`。
- [x] 5.3 验证 `pnpm check`、`pnpm test`、`pnpm demo:api:test`、`git diff --check` 和 `openspec validate agenthub-p18b-memory-effectiveness-rehearsal --strict`。
- [x] 5.4 确认 P18b 不会增加自动长期学习、embeddings/RRF/graph 检索、提供商市场、生产部署或护栏绕过功能。
